<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Successful | Website Health Monitor</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="wrapper">
        <!-- Page Content -->
        <div class="content full-width">
            <!-- Main Content -->
            <main class="container-fluid py-4">
                <div class="row justify-content-center">
                    <div class="col-md-6 col-lg-5">
                        <div class="card shadow-sm">
                            <div class="card-body p-5 text-center">
                                <div class="mb-4">
                                    <i class="fas fa-check-circle fa-4x text-success"></i>
                                </div>
                                <h1 class="h3 mb-3">Login Successful</h1>
                                <p class="text-muted mb-4">You have successfully logged in. Redirecting to dashboard...</p>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Store token in localStorage and redirect -->
    <script>
        // Store token in localStorage
        localStorage.setItem('authToken', '<%= token %>');
        
        // Set token as cookie for server-side access
        document.cookie = `authToken=<%= token %>; path=/; max-age=${60*60*24*7}`; // 7 days
        
        // Redirect to dashboard after 2 seconds
        setTimeout(function() {
            window.location.href = '/dashboard';
        }, 2000);
    </script>
</body>
</html> 